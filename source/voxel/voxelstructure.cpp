/* Created by David Klostermann on 19.06.2017. */
#include "voxelstructure.h"
/*
VoxelStructure::VoxelStructure() {

}

bool VoxelStructure::voxelExists(glm::tvec3<int16_t> position) {
    return std::find(m_allExistingVoxels.begin(), m_allExistingVoxels.end(), position) != m_allExistingVoxels.end();
}

Voxel VoxelStructure::getVoxel(glm::tvec3<int16_t> position) {
    return m_voxels.at(position.x).at(position.y).at(position.z);
}

void VoxelStructure::setVoxel(glm::tvec3<int16_t> position, Voxel voxel) {
    m_allExistingVoxels.push_back(position);
    m_voxels[position.x][position.y][position.z] = voxel;
}

const std::vector<glm::tvec3<int16_t>>& VoxelStructure::getVoxelPositions() {
    return m_allExistingVoxels;
}
 */